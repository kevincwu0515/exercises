INT f2(INT x, INT y)
BEGIN
	z := x*x - y*y;
	RETURN z;
END
INT MAIN f1()
BEGIN
	INT x;
	READ(x, "testcase/test1.input");
	INT y;
	READ(y, "testcase/test2.input");
	INT z;
	z := f2(x,y) + f2(y,x);
	WRITE(z, "A1.output");
END
